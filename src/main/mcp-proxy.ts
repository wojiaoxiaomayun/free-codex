/** @deprecated The standalone proxy layer was removed; codex-mcp now owns the MCP HTTP server. */
export { NodeMcpGateway as McpProxy } from './mcp-gateway'
export type { GatewayEvent as ProxyEvent } from './mcp-gateway'
